# Данные и решения в репозиторий не коммитим — они грузятся через сайдбар.
# Если у вас ПРИВАТНЫЙ репозиторий и вы намеренно хотите положить выгрузки в
# папку data/ и коммитить их — удалите соответствующие строки ниже.
data/
Результат*.xlsx
Решения.xlsx

# Служебное
__pycache__/
*.pyc
*.pyo
.DS_Store
.venv/
venv/
.streamlit/secrets.toml
